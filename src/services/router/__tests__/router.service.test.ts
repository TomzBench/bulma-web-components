import 'reflect-metadata'; // TODO why is this not imported already
// import { parse } from '../router.service';
import sinon from 'sinon';

describe('router.service', () => {
  it('should parse', () => {
    // let result = parse('/foo/:bar/abcd/users/:user');
    // console.log(result);
  });
});
